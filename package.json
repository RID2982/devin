{
  "name": "event-management-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server",
    "database",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w database && npm run build -w server && npm run build -w client",
    "lint": "npm run lint -w server && npm run lint -w client",
    "db:generate": "npm run db:generate -w database",
    "db:migrate": "npm run db:migrate -w database",
    "db:seed": "npm run db:seed -w database",
    "db:reset": "npm run db:reset -w database",
    "db:studio": "npm run db:studio -w database"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=20"
  }
}
