/**
 * Drop your final logo file into client/public/ (PNG, SVG, JPG, or WebP all work)
 * and point this at it, e.g. LOGO_SRC = '/logo.png'. Leave undefined to keep the
 * wordmark placeholder rendered by <Logo /> (see components/shared/Logo.tsx).
 */
export const LOGO_SRC: string | undefined = '/logo.png';
